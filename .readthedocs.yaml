version: 2

python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"
    
formats:
  - pdf
  - epub
    
sphinx:
  fail_on_warning: true
